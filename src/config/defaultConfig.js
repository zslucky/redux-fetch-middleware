'use strict';

/*
 *
 *  Default global config
 *
 */
const defaultConifg = {
    suffix: ['REQUEST', 'SUCCESS', 'FAILURE'],
    fetchOptions: {},
    cache: {
        enable: false
    }
};

export default defaultConifg;
